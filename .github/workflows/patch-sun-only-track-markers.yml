name: Patch sun-only track markers
on:
  push:
    branches:
      - feature/celestial-track-events-v2
    paths:
      - scripts/patch-sun-only-track-markers.py
      - .github/workflows/patch-sun-only-track-markers.yml
permissions:
  contents: write
jobs:
  patch-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/celestial-track-events-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Apply sun-only patch
        run: python3 scripts/patch-sun-only-track-markers.py
      - run: npm ci
      - run: npm run build
      - name: Commit feature
        run: |
          git config user.name "wardolb-bit"
          git config user.email "wardolb@users.noreply.github.com"
          git add components/NavMapMainOverlayV2.tsx
          git commit -m "Keep sunrise and sunset markers on track"
          git pull --rebase origin feature/celestial-track-events-v2
          git push origin HEAD:feature/celestial-track-events-v2
