"use client";

import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { decodeFieldValues, parseFields, parseProduct, splitMessages } from "@azohra/meteo.grib";

function u32(a,o){return new DataView(a.buffer,a.byteOffset+o,4).getUint32(0,false)}
function i32(a,o){return new DataView(a.buffer,a.byteOffset+o,4).getInt32(0,false)}
function u16(a,o){return new DataView(a.buffer,a.byteOffset+o,2).getUint16(0,false)}
function normalizeLon(lon){let x=lon;while(x>180)x-=360;while(x< -180)x+=360;return x}
function nearLon(lon,ref){let x=lon;while(x-ref>180)x-=360;while(x-ref< -180)x+=360;return x}
function gridInfo(s){const t=u16(s,12);if(t!==0)throw new Error(`GRIB grid template 3.${t} is not supported yet.`);const b=u32(s,38),sub=u32(s,42),unit=b===0||sub===0xffffffff?1e-6:b/sub;return{ni:u32(s,30),nj:u32(s,34),lat1:i32(s,46)*unit,lon1:i32(s,50)*unit,di:u32(s,63)*unit,dj:u32(s,67)*unit,scan:s[71]}}
function forecastHour(s){if(s.length<22)return 0;const unit=s[17],raw=i32(s,18);if(unit===0)return raw/60;if(unit===1)return raw;if(unit===2)return raw*24;if(unit===10)return raw*3;if(unit===11)return raw*6;if(unit===12)return raw*12;if(unit===13)return raw/3600;return raw}
function fieldName(d,c,n){const k=`${d}.${c}.${n}`;return({"10.0.3":"Significant wave height","10.0.10":"Primary wave direction","10.0.11":"Primary wave period","10.0.8":"Wind-wave height","10.0.9":"Wind-wave period","0.2.2":"U wind","0.2.3":"V wind"})[k]||`Parameter ${k}`}
function waveColor(m){if(m>=4)return"#ef4444";if(m>=3)return"#f59e0b";if(m>=2)return"#eab308";if(m>=1)return"#22c55e";return"#22d3ee"}

export default function GribViewer(){
 const mapEl=useRef(null),mapRef=useRef(null),layerRef=useRef(null),recordsRef=useRef([]);
 const[fileName,setFileName]=useState("");const[hours,setHours]=useState([]);const[hourIndex,setHourIndex]=useState(0);const[inventory,setInventory]=useState([]);const[status,setStatus]=useState("Load a GRIB2 file to begin.");const[showSeas,setShowSeas]=useState(true);const[showWaveDir,setShowWaveDir]=useState(true);const[showWind,setShowWind]=useState(true);const selectedHour=hours[hourIndex]??0;
 useEffect(()=>{let cancelled=false;(async()=>{if(!mapEl.current||mapRef.current)return;const L=await import("leaflet");if(cancelled||!mapEl.current)return;if(!document.querySelector('link[data-grib-leaflet="true"]')){const link=document.createElement("link");link.rel="stylesheet";link.href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css";link.setAttribute("data-grib-leaflet","true");document.head.appendChild(link)}const map=L.map(mapEl.current,{attributionControl:false}).setView([20,180],3);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{maxZoom:19}).addTo(map);layerRef.current=L.layerGroup().addTo(map);mapRef.current=map;setTimeout(()=>map.invalidateSize(),100)})();return()=>{cancelled=true;mapRef.current?.remove();mapRef.current=null}},[]);
 async function loadFile(file){setStatus("Reading GRIB2 fields…");try{const bytes=new Uint8Array(await file.arrayBuffer()),records=[];for(const message of splitMessages(bytes)){for(const field of parseFields(message)){const p=parseProduct(field.section4);records.push({discipline:field.discipline,category:p.parameterCategory,number:p.parameterNumber,forecastHour:forecastHour(field.section4),field})}}if(!records.length)throw new Error("No GRIB2 fields found.");recordsRef.current=records;const hs=Array.from(new Set(records.map(r=>r.forecastHour))).sort((a,b)=>a-b);setFileName(file.name);setHours(hs);setHourIndex(0);setInventory(Array.from(new Set(records.map(r=>fieldName(r.discipline,r.category,r.number)))));setStatus(`${records.length} fields loaded • ${hs.length} forecast times.`)}catch(e){recordsRef.current=[];setHours([]);setInventory([]);setFileName("");setStatus(e instanceof Error?e.message:"Could not read GRIB2 file.")}}
 useEffect(()=>{let cancelled=false;(async()=>{const map=mapRef.current,layer=layerRef.current;if(!map||!layer||!recordsRef.current.length)return;const L=await import("leaflet");layer.clearLayers();const at=recordsRef.current.filter(r=>Math.abs(r.forecastHour-selectedHour)<.001),find=(d,c,n)=>at.find(r=>r.discipline===d&&r.category===c&&r.number===n);const sig=find(10,0,3),dir=find(10,0,10),per=find(10,0,11),u=find(0,2,2),v=find(0,2,3),anchor=sig||dir||u||v;if(!anchor){setStatus(`F${String(selectedHour).padStart(3,"0")}: no supported wind/wave fields.`);return}try{const g=gridInfo(anchor.field.section3),sv=sig?decodeFieldValues(sig.field).values:null,dv=dir?decodeFieldValues(dir.field).values:null,pv=per?decodeFieldValues(per.field).values:null,uv=u?decodeFieldValues(u.field).values:null,vv=v?decodeFieldValues(v.field).values:null,stride=Math.max(1,Math.ceil(Math.sqrt(g.ni*g.nj/900))),ip=(g.scan&0x80)===0,jp=(g.scan&0x40)!==0,ll=[];for(let j=0;j<g.nj;j+=stride)for(let i=0;i<g.ni;i+=stride){const idx=j*g.ni+i,lat=g.lat1+(jp?1:-1)*j*g.dj,lon=nearLon(normalizeLon(g.lon1+(ip?1:-1)*i*g.di),normalizeLon(g.lon1));ll.push([lat,lon]);const h=Number(sv?.[idx]),d=Number(dv?.[idx]),p=Number(pv?.[idx]),uu=Number(uv?.[idx]),vvv=Number(vv?.[idx]);if(showSeas&&Number.isFinite(h))L.circleMarker([lat,lon],{radius:6,weight:0,fillOpacity:.62,fillColor:waveColor(h)}).bindTooltip(`<b>Significant seas:</b> ${(h*3.28084).toFixed(1)} ft${Number.isFinite(p)?`<br/><b>Primary period:</b> ${p.toFixed(0)} s`:""}${Number.isFinite(d)?`<br/><b>Primary direction:</b> ${d.toFixed(0)}°`:""}`).addTo(layer);if(showWaveDir&&Number.isFinite(d)&&(i/stride+j/stride)%3===0)L.marker([lat,lon],{interactive:false,icon:L.divIcon({className:"",html:`<div style="transform:rotate(${d}deg);color:#f8fafc;font-size:18px;text-shadow:0 1px 3px #000">↑</div>`,iconSize:[18,18],iconAnchor:[9,9]})}).addTo(layer);if(showWind&&Number.isFinite(uu)&&Number.isFinite(vvv)&&(i/stride+j/stride)%4===0){const sp=Math.hypot(uu,vvv)*1.94384,from=(Math.atan2(-uu,-vvv)*180/Math.PI+360)%360;L.marker([lat,lon],{icon:L.divIcon({className:"",html:`<div title="${sp.toFixed(0)} kt" style="transform:rotate(${from}deg);color:#67e8f9;font-size:17px;text-shadow:0 1px 3px #000">↑</div>`,iconSize:[18,18],iconAnchor:[9,9]})}).addTo(layer)}}if(!cancelled&&ll.length)map.fitBounds(L.latLngBounds(ll),{padding:[24,24]});setStatus(`F${String(selectedHour).padStart(3,"0")} rendered • ${g.ni}×${g.nj} grid • ${sig?"seas ":""}${dir?"wave-dir ":""}${per?"period ":""}${u&&v?"wind":""}`.trim())}catch(e){setStatus(e instanceof Error?e.message:"Could not render this GRIB frame.")}})();return()=>{cancelled=true}},[selectedHour,showSeas,showWaveDir,showWind]);
 const validLabel=useMemo(()=>`F${String(Math.round(selectedHour)).padStart(3,"0")}`,[selectedHour]);
 return <main className="min-h-screen bg-[#04080c] p-2 text-slate-100"><div className="mb-2 flex flex-wrap items-center justify-between gap-2 border border-amber-500/25 bg-[#071019] px-3 py-2"><div><div className="text-[10px] font-black uppercase tracking-[0.18em] text-[#c9a227]">NAVDASH GRIB VIEWER</div><div className="text-sm font-black">Local GRIB2 weather and sea-state overlay</div></div><Link href="/route-weather-lab" className="border border-[#c9a227]/50 bg-[#101820] px-3 py-2 text-[10px] font-black text-[#f1d56b]">ROUTE WEATHER</Link></div><section className="mb-2 border border-slate-700/50 bg-[#071019] p-3"><div className="flex flex-wrap items-center gap-2"><label className="cursor-pointer border border-cyan-400/40 bg-[#08131b] px-3 py-2 text-[10px] font-black text-cyan-200">LOAD GRIB<input type="file" accept=".grb,.grib,.grb2,.grib2,application/octet-stream" className="hidden" onChange={e=>e.target.files?.[0]&&loadFile(e.target.files[0])}/></label><div className="text-xs font-black text-slate-200">{fileName||"No GRIB loaded"}</div><div className="ml-auto text-[10px] font-black text-[#f1d56b]">{hours.length?validLabel:"--"}</div></div>{hours.length>1&&<div className="mt-3"><input className="w-full accent-amber-400" type="range" min={0} max={hours.length-1} value={hourIndex} onChange={e=>setHourIndex(Number(e.target.value))}/><div className="mt-1 flex justify-between text-[9px] text-slate-500"><span>F{String(Math.round(hours[0])).padStart(3,"0")}</span><span>FORECAST TIME</span><span>F{String(Math.round(hours[hours.length-1])).padStart(3,"0")}</span></div></div>}<div className="mt-3 flex flex-wrap gap-2"><label className="flex items-center gap-2 border border-slate-700 bg-[#050a0f] px-2 py-2 text-[10px] font-black"><input type="checkbox" checked={showSeas} onChange={e=>setShowSeas(e.target.checked)}/> SEAS</label><label className="flex items-center gap-2 border border-slate-700 bg-[#050a0f] px-2 py-2 text-[10px] font-black"><input type="checkbox" checked={showWaveDir} onChange={e=>setShowWaveDir(e.target.checked)}/> WAVE DIR</label><label className="flex items-center gap-2 border border-slate-700 bg-[#050a0f] px-2 py-2 text-[10px] font-black"><input type="checkbox" checked={showWind} onChange={e=>setShowWind(e.target.checked)}/> WIND</label></div></section><div className="grid gap-2 xl:grid-cols-[minmax(0,1fr)_300px]"><section className="overflow-hidden border border-slate-800"><div ref={mapEl} style={{width:"100%",height:"68vh",minHeight:520,background:"#0a141d"}}/></section><aside className="space-y-2"><section className="border border-slate-700/50 bg-[#071019] p-3"><div className="text-[9px] font-black uppercase tracking-[0.14em] text-slate-500">GRIB CONTENTS</div><div className="mt-2 space-y-1 text-[10px] text-slate-300">{inventory.length?inventory.map(x=><div key={x}>• {x}</div>):<div>No fields loaded.</div>}</div></section><section className="border border-slate-700/50 bg-[#071019] p-3"><div className="text-[9px] font-black uppercase tracking-[0.14em] text-slate-500">STATUS</div><div className="mt-2 text-[11px] leading-relaxed text-slate-300">{status}</div></section></aside></div></main>
}
